# Environment Configuration Example
# Copy this file to .env and update with your settings
#
# IMPORTANT: Never commit .env to git - it contains secrets!
# The .env file is already in .gitignore

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Secret key for Flask sessions (MUST change in production!)
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production-use-secrets-token-hex

# Database URL
# Development (SQLite - default): Uses data/recipes.db automatically
# Production (PostgreSQL): Set DATABASE_URL environment variable on your hosting platform
#
# Examples:
# - Railway: postgresql://user:password@host.railway.app:5432/railway
# - Render: postgresql://user:password@host.render.com:5432/dbname
# - Heroku: postgres://user:password@host.amazonaws.com:5432/dbname
#
# Leave commented for development (uses SQLite by default)
# DATABASE_URL=postgresql://user:password@localhost:5432/recipe_manager

# ============================================================================
# AI API KEYS (Optional - can also be set via Admin Panel)
# ============================================================================

# Groq API Key for recipe translation and ingredient substitution
# Get your key at: https://console.groq.com/
# Note: You can also set this in the Admin panel instead
GROQ_API_KEY=your-groq-api-key-here

# Mistral API Key (alternative AI provider)
# Get your key at: https://console.mistral.ai/
# Note: You can also set this in the Admin panel instead
MISTRAL_API_KEY=your-mistral-api-key-here

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# NYT Cookie for scraping premium recipes (optional)
# This can also be set via the Admin panel
# NYT_S_COOKIE=your-nyt-cookie-here

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# For Production Deployment:
# 1. Set DATABASE_URL to your managed PostgreSQL database
# 2. Set SECRET_KEY to a strong random value
# 3. Set API keys (or use Admin panel to configure them)
# 4. Never use SQLite in production - use PostgreSQL instead
#
# Recommended hosting platforms with managed databases:
# - Railway (https://railway.app) - Free PostgreSQL included
# - Render (https://render.com) - Free PostgreSQL included
# - Heroku (https://heroku.com) - PostgreSQL addon available
# - Fly.io (https://fly.io) - PostgreSQL addon available
